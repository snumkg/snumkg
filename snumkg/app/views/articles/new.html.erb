
<%= form_for(@article) do |f| %>

  <%= f.label :title, '제목' %>
  <%= f.text_field :title %>

  <%= f.label :body, '내용' %>
  <%= f.text_area :body %>

  <br />
  <%= f.submit "글쓰기", class:"btn" %>

<% end %>
