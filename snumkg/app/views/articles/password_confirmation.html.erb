<%= form_tag article_path(group_id: params[:group_id], board_id: params[:board_id], id: params[:id]), :method => :delete  do  %>
  
  <%= label_tag :password, "비밀번호 확인" %>
  <%= password_field_tag :password %>
  <%= hidden_field_tag :group_id, params[:group_id] %>
  <%= hidden_field_tag :board_id, params[:board_id] %>
  <%= hidden_field_tag :id, params[:id] %>
  <%= submit_tag  "submit"%>
<% end %>

<% else %>

