<%= form_tag article_path(group_name: params[:group_name], board_name: params[:board_name], id: params[:id]), :method => :delete  do  %>
  
  <%= label_tag :password, "비밀번호 확인" %>
  <%= password_field_tag :password %>
  <%= hidden_field_tag :group_name, params[:group_name] %>
  <%= hidden_field_tag :board_name, params[:board_name] %>
  <%= hidden_field_tag :id, params[:id] %>
  <%= submit_tag  "submit"%>
<% end %>

<% else %>

