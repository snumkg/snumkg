<div id="tab_sidebar">
  <ul>
    <% for board in @boards %>
      <li><%= link_to board.name, articles_path(:tab_name => params[:tab_name], :board_name => board.name) %></li>
    <% end %>
  </ul>
</div>

